Flask==3.0.3
pypdf==4.0.1
groq==0.9.0
# Compatibility fix: groq==0.9.0 expects httpx that still supports the `proxies` kwarg internally.
# Your environment currently has httpx==0.28.1 which triggers: Client.__init__() got an unexpected keyword argument 'proxies'
httpx==0.23.3

httpcore==0.16.3


gTTS==2.5.1
python-dotenv==1.0.1

